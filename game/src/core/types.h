#pragma once

#include "pch.h"

using String = std::string;

template<typename T>
using Vector = std::vector<T>;

using Vec2 = glm::vec2;

using Vec3 = glm::vec3;

using Path = std::filesystem::path;

using Quat = glm::quat;