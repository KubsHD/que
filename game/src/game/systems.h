#pragma once
#include <entt/entt.hpp>
#include <common/GraphicsAPI_Vulkan.h>

struct render_mesh_system {
	void update(entt::registry& reg)
	{

	}

	void render(entt::registry& reg, GraphicsAPI_Vulkan& gapi)
	{

	}
};